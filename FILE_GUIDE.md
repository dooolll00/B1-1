# B1-1 파일 안내 — 어디부터 읽으면 될까요?

처음에는 **index.html → css/style.css → js/main.js** 세 파일만 순서대로 읽어도 됩니다. 각각 **내용 → 모양 → 동작**을 담당합니다. 나머지는 이미지, 설명 문서, 개발 설정, 검사 도구입니다.

## 1. 사이트 화면을 만드는 파일

| 파일 | 무엇을 위한 파일인가요? | 어떤 경우에 보나요? |
| --- | --- | --- |
| [index.html](index.html) | 인사말·소개·기술 목록·프로젝트 영역·문의 폼·하단 정보의 구조입니다. CSS·JS·이미지를 연결하는 시작 파일입니다. | 소개 글, 메뉴 이름, 입력칸, 섹션 구성을 바꿀 때 |
| [css/style.css](css/style.css) | 색상·폰트·간격·배치·다크 모드 색상·모바일 화면·애니메이션을 정합니다. | 디자인이나 화면 크기별 배치를 바꿀 때 |
| [js/main.js](js/main.js) | 메뉴 클릭, 테마 전환·저장, 스크롤, GitHub 조회·필터, 폼 검증을 처리합니다. | 버튼 동작이나 데이터 처리 방식을 바꿀 때 |
| [images/profile.png](images/profile.png) | 자기소개에 표시하는 프로필 이미지입니다. | 프로필 이미지를 바꿀 때 |
| [images/favicon.svg](images/favicon.svg) | 브라우저 탭에 표시하는 작은 아이콘입니다. SVG는 도형을 글 형태의 코드로 표현한 이미지 형식입니다. | 탭 아이콘을 바꿀 때 |

브라우저가 index.html을 읽고, 연결된 CSS로 모양을 정한 뒤 JS로 동작을 처리합니다. 예를 들어 **HTML의 테마 버튼 → JS가 클릭 처리 → CSS가 다른 색상 적용**으로 연결됩니다.

## 2. 설명과 학습을 위한 문서

`.md`는 제목·표·목록을 작성할 수 있는 Markdown 문서입니다. VS Code에서 파일을 열고 **미리 보기 열기**를 선택하면 표를 읽기 편합니다. macOS에서는 `Shift + Command + V`로 미리 보기를 열 수 있습니다.

| 파일 | 역할 | 읽을 때 |
| --- | --- | --- |
| [README.md](README.md) | 프로젝트 대표 안내문입니다. 소개·사용 기술·요구사항 체크·검증·배포 주소를 설명합니다. | 프로젝트 전체를 소개하거나 실행·배포 방법을 찾을 때 |
| [FILE_GUIDE.md](FILE_GUIDE.md) | 지금 읽고 있는 파일별 역할 안내입니다. | 무엇을 열어야 할지 모를 때 |
| [MISSION_GUIDE.md](MISSION_GUIDE.md) | HTML→CSS→JS 기능을 단계별로 이해하는 학습 안내입니다. | 코드를 공부하고 구현 순서를 이해할 때 |
| [PRESENTATION_GUIDE.md](PRESENTATION_GUIDE.md) | 쉬운 설명·발표 대본·시연 순서·예상 질문·미션 전체 확인표입니다. | 다른 사람에게 설명할 준비를 할 때 |
| [WORK_LOG.md](WORK_LOG.md) | 사용자의 요청, 변경 내용, 검사 결과, 남은 일을 시간순으로 기록합니다. 제공된 미션 요구사항 요약도 있습니다. | 이전에 무엇을 했고 어디서 이어갈지 확인할 때 |
| [AGENTS.md](AGENTS.md) | 이 저장소에서 Codex가 지켜야 할 작업 규칙입니다. 예: 기존 기록 읽기, 작업 후 기록 추가. | AI에게 적용되는 프로젝트 작업 규칙을 확인할 때 |

문서가 웹사이트의 버튼을 움직이거나 색상을 바꾸지는 않습니다. 서로 목적이 다르므로 처음부터 전부 외울 필요는 없습니다.

## 3. review.py는 왜 있나요?

[tests/review.py](tests/review.py)는 **사람 대신 Chrome에서 버튼을 누르고 결과를 확인하는 자동 검사 파일**입니다. 앞선 미션 검토 과정에서 같은 검사를 반복할 수 있도록 추가했습니다.

예를 들어 사람이 “다크 모드를 누르고 새로고침해도 유지되는지” 확인하는 행동을 코드로 적어 둔 것입니다. 코드를 정리한 뒤 기존 기능이 망가지지 않았는지 확인할 때 사용합니다.

- Python: `.py` 파일을 실행하는 언어입니다.
- Playwright: 브라우저를 열고 클릭·입력을 자동으로 수행하는 도구입니다.
- 모의 응답: GitHub가 실제로 답하는 대신 검사 도구가 미리 준비한 데이터를 돌려주는 방식입니다. 빈 목록이나 403 오류를 일정하게 재현할 수 있습니다.
- `assert` / `expect`: “이 결과가 맞는지 확인하라”는 검사 표현입니다.
- `PASS`: 해당 검사 통과. `ALL PASS`: 이번 실행의 모든 검사 통과. 모든 기기·상황을 무조건 보장한다는 뜻은 아닙니다.

**사이트 방문자의 브라우저는 이 Python 파일을 실행하지 않습니다.** index.html에도 연결하지 않았습니다. 미션의 순수 HTML/CSS/JavaScript 조건은 사이트 구현에 적용되며, 이 파일은 개발용 검사 도구입니다. 미션 원문에서 이 Python 자동 검사 파일을 필수 제출물로 요구하지는 않습니다. 핵심 학습은 HTML·CSS·JS에 집중하면 됩니다.

### 파일을 읽는 순서

review.py에 번호와 한국어 주석을 붙였습니다. 먼저 주석으로 어떤 기능을 검사하는지만 읽어도 됩니다.

| 번호 | 검사하는 내용 |
| --- | --- |
| 01~02 | 검사 주소·연습 데이터·공통 검사 준비 |
| 03~05 | HTML 구조, 여러 화면 크기, 메뉴·스크롤 |
| 06~08 | 다크 모드 저장, 폼 오류·성공, 언어 필터 |
| 09~10 | API 로딩·성공·빈 결과·오류·재시도 |
| 11~14 | 캐시, 여러 페이지 조회, 외부 데이터 처리, 시간 제한·등장 효과 |
| 15 | 선택 실행: 실제 GitHub·공개 사이트 확인과 스크린샷 갱신 |
| 16 | 터미널에서 실행을 시작하는 부분 |

### 일반 검사와 실제 서비스 검사의 차이

| 실행 방식 | 무엇을 하나요? | 파일에 미치는 영향 |
| --- | --- | --- |
| `python tests/review.py` | 로컬 사이트에서 GitHub API를 모의 응답으로 바꿔 검사합니다. | 제출용 스크린샷을 갱신하지 않습니다. |
| `python tests/review.py --live` | 위 검사에 더해 실제 GitHub API와 기존 공개 Pages도 확인합니다. | 로컬 화면을 캡처하여 기존 스크린샷 3개를 덮어씁니다. |

`--live`는 배포 명령이 아닙니다. 로컬 코드와 공개 코드가 같은지 판정하는 명령도 아닙니다. 일반 검사도 로컬 서버와 Chrome이 필요하며 이미지·외부 웹 폰트 등은 실제 페이지 경로로 로드됩니다.

### 필요할 때 실행하는 방법

B1-1 폴더의 터미널에서 실행합니다. Python 3, Google Chrome, Playwright가 필요합니다. 다음은 macOS에서 임시 가상환경을 사용하는 방법입니다. 가상환경은 프로젝트 코드와 별도로 검사 도구를 설치하는 공간입니다.

```bash
python3 -m venv /tmp/b1-1-review-venv
/tmp/b1-1-review-venv/bin/pip install playwright
python3 -m http.server 5511 --bind 127.0.0.1
```

서버 터미널을 켜 둔 채 **다른 터미널**을 B1-1에서 열고 실행합니다.

```bash
/tmp/b1-1-review-venv/bin/python tests/review.py
```

완료 후 서버 터미널에서 `Control + C`로 서버를 종료합니다. 검사 전용 서버는 **5511번**, 평소 Live Server 설정은 **5500번**입니다. 5500 서버만 켜 두면 현재 검사 코드가 접속할 5511 서버는 없는 상태입니다.

일반 실행 결과에 `ALL PASS: 27 checks`가 나오는 것이 현재 코드의 기준입니다. 검사 중 오류가 나면 마지막 오류와 어느 검사까지 PASS였는지 확인합니다. Python 모듈 오류는 도구 설치, 접속 오류는 서버 주소·포트, 브라우저 실행 오류는 Chrome 설치 상태부터 확인합니다. 기능 검사 실패는 실제 사이트 동작이나 테스트 조건을 살펴봐야 합니다.

## 4. 설정·배포·Git 관련 파일

| 파일·폴더 | 쉬운 설명 | 현재 내용·역할 |
| --- | --- | --- |
| [.vscode/settings.json](.vscode/settings.json) | 이 프로젝트의 VS Code 설정입니다. JSON은 설정값을 키와 값으로 적는 형식입니다. | Live Server 주소 127.0.0.1, 포트 5500 |
| [.vscode/extensions.json](.vscode/extensions.json) | 추천하는 VS Code 확장 목록입니다. | Live Server 추천. 추천 목록만 있다고 자동 설치되지는 않습니다. |
| [.gitignore](.gitignore) | Git에서 새로 추적하지 않을 파일 규칙입니다. | 현재 macOS가 만드는 .DS_Store를 제외합니다. 이미 추적 중인 파일을 자동 삭제하지는 않습니다. |
| [.nojekyll](.nojekyll) | GitHub Pages에서 Jekyll 처리를 건너뛰라는 표시입니다. | 내용이 비어 있어도 파일이 있는 것 자체로 의미가 있습니다. 디자인 파일이 아닙니다. |
| .git/ | Git이 변경 이력과 저장소 정보를 관리하는 내부 폴더입니다. | 일반 작업에서 직접 편집하는 곳이 아닙니다. |

점으로 시작하는 이름은 설정이나 숨김 파일인 경우가 많습니다. 사이트 내용을 바꾸려면 우선 HTML·CSS·JS를 확인하세요.

## 5. 제출용 화면 이미지

| 파일 | 내용 |
| --- | --- |
| [images/screenshots/desktop.png](images/screenshots/desktop.png) | 데스크톱 크기의 전체 화면 |
| [images/screenshots/mobile.png](images/screenshots/mobile.png) | 모바일 크기의 전체 화면 |
| [images/screenshots/dark.png](images/screenshots/dark.png) | 다크 모드 전체 화면 |

README와 미션 제출에 사용하는 결과 이미지입니다. 이 이미지를 바꿔도 실제 사이트 디자인은 바뀌지 않습니다. 실제 화면을 바꾼 후 필요하면 새로 캡처합니다.

## 6. 목적에 따라 이렇게 찾아보세요

| 하고 싶은 일 | 먼저 열 파일 |
| --- | --- |
| 내 소개 글을 바꾸기 | index.html |
| 색상·글꼴·간격 바꾸기 | css/style.css |
| 메뉴·테마·폼 동작 이해하기 | js/main.js의 02~06 번호 주석 |
| GitHub 프로젝트 조회 이해하기 | js/main.js의 loadProjects → fetchAllRepos / renderProjects |
| 미션을 순서대로 공부하기 | MISSION_GUIDE.md |
| 다른 사람에게 설명하기 | PRESENTATION_GUIDE.md |
| 수정 후 기능이 유지되는지 검사하기 | tests/review.py |
| 지난 작업과 미션 요약 확인하기 | WORK_LOG.md |
| 사이트 주소·배포 방법 찾기 | README.md |

처음 연습할 때는 **화면에서 버튼 하나 고르기 → HTML에서 찾기 → JS에서 클릭 처리 찾기 → CSS에서 모양 찾기** 순서로 읽어 보세요. 파일들이 서로 어떻게 연결되는지 이해하기 쉬워집니다.
